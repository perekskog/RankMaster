{
  "entities": {
    "Category": {
      "title": "Category",
      "type": "object",
      "description": "Represents a category of products.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the category."
        },
        "description": {
          "type": "string",
          "description": "A short description of the category."
        },
        "userId": {
          "type": "string",
          "description": "ID of the user who owns this category."
        }
      },
      "required": [
        "id",
        "name",
        "userId"
      ]
    },
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Represents a product within a category.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "categoryId": {
          "type": "string",
          "description": "ID of the category this product belongs to."
        },
        "name": {
          "type": "string",
          "description": "Name of the product."
        },
        "description": {
          "type": "string",
          "description": "Text description of the product."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the product's picture.",
          "format": "uri"
        },
        "imageHint": {
          "type": "string",
          "description": "Hint for AI to find a better image."
        },
        "userId": {
          "type": "string",
          "description": "ID of the user who owns this product."
        }
      },
      "required": [
        "id",
        "categoryId",
        "name",
        "userId"
      ]
    },
    "GradedRank": {
      "title": "GradedRank",
      "type": "object",
      "description": "Represents a graded ranking of a product.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the GradedRank entity."
        },
        "productId": {
          "type": "string",
          "description": "ID of the product being ranked."
        },
        "categoryId": {
          "type": "string",
          "description": "ID of the category the product belongs to."
        },
        "rank": {
          "type": "number",
          "description": "The grade given to the product."
        },
        "userId": {
          "type": "string",
          "description": "ID of the user who created this ranking."
        }
      },
      "required": [
        "id",
        "productId",
        "categoryId",
        "rank",
        "userId"
      ]
    },
    "ComparativeRank": {
      "title": "ComparativeRank",
      "type": "object",
      "description": "Represents a comparative ranking between two products.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ComparativeRank entity."
        },
        "categoryId": {
          "type": "string",
          "description": "ID of the category the products belong to."
        },
        "winnerProductId": {
          "type": "string",
          "description": "ID of the winning product."
        },
        "loserProductId": {
          "type": "string",
          "description": "ID of the losing product."
        },
        "userId": {
          "type": "string",
          "description": "ID of the user who created this ranking."
        }
      },
      "required": [
        "id",
        "categoryId",
        "winnerProductId",
        "loserProductId",
        "userId"
      ]
    },
    "ProductRank": {
      "title": "ProductRank",
      "type": "object",
      "description": "Stores the calculated rank for a product within a category.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Combination of categoryId and productId."
        },
        "categoryId": {
          "type": "string",
          "description": "ID of the category."
        },
        "productId": {
          "type": "string",
          "description": "ID of the product."
        },
        "score": {
          "type": "number",
          "description": "The combined ranking score."
        },
        "userId": {
          "type": "string",
          "description": "ID of the user who owns this rank data."
        }
      },
      "required": [
        "id",
        "categoryId",
        "productId",
        "score",
        "userId"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/categories/{categoryId}": {
      "schema": {
        "$ref": "#/entities/Category"
      },
      "description": "Stores all product categories."
    },
    "/products/{productId}": {
      "schema": {
        "$ref": "#/entities/Product"
      },
      "description": "Stores all products, queryable by categoryId."
    },
    "/gradedRankings/{rankingId}": {
      "schema": {
        "$ref": "#/entities/GradedRank"
      },
      "description": "Stores all graded rankings, queryable by categoryId or productId."
    },
    "/comparativeRankings/{rankingId}": {
      "schema": {
        "$ref": "#/entities/ComparativeRank"
      },
      "description": "Stores all comparative rankings, queryable by categoryId."
    },
    "/productRanks/{rankId}": {
      "schema": {
        "$ref": "#/entities/ProductRank"
      },
      "description": "Stores the calculated ranks for products."
    }
  }
}